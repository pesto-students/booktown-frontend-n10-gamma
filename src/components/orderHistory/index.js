import React from 'react';
import OrderHistoryComponent from './component';

const Index = (props) => {
  return <OrderHistoryComponent {...props} />;
};

Index.propTypes = {};

export default Index;
